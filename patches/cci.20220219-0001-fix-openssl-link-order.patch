--- util/BUILD.gn
+++ util/BUILD.gn
@@ -668,8 +668,8 @@ crashpad_static_library("net") {
         deps += [ "//third_party/boringssl" ]
       } else {
         libs = [
-          "crypto",
           "ssl",
+          "crypto",
         ]
       }
     }
@@ -714,8 +714,8 @@ if (!crashpad_is_android && !crashpad_is_ios) {
         deps += [ "//third_party/boringssl" ]
       } else {
         libs = [
-          "crypto",
           "ssl",
+          "crypto",
         ]
       }
     }
